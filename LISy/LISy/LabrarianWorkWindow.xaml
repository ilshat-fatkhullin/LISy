<Window x:Class="LISy.LabrarianWorkWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LISy"
        mc:Ignorable="d"
        Title="LabrarianWorkWindow" Height="423" Width="970" Loaded="LabrarianWorkWindow_OnLoaded">
    <Grid Margin="0,0,0,47">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="0" />
            <RowDefinition Height="0"/>
        </Grid.RowDefinitions>
        <DataGrid AutoGenerateColumns="False" x:Name="LibraryGrid" Margin="0,0,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Title}" Header="ID BOOK" Width="120"/>
                <DataGridTextColumn Binding="{Binding Company}" Header="ID Patron" Width="125"/>

            </DataGrid.Columns>
        </DataGrid>
        <Button x:Name="button" Content="Refresh" Margin="434,0,429,-40" Grid.Row="1" VerticalAlignment="Bottom" Height="30" Grid.RowSpan="2" Click="button_Click"/>
    </Grid>
</Window>
